{
  "name": "spraff",
  "version": "1.0.0",
  "description": "Simple AI voice chat",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:mic-bleed": "playwright test tests/e2e/mic-bleed.spec.js",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "whisper-node": "^1.1.0"
  }
}
